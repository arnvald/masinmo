.comment
  %p
    = comment.user.try(:full_name)
    = comment.created_at
  %p= comment.body
  - if current_user && current_user.admin?
    = link_to 'Delete comment', offer_comment_path(@offer, comment), method: :delete
